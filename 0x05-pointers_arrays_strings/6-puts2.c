#include "main.h"

/**
 * puts2 - takes a pointerand updates its value.
 * @str: input.
 * Return: no return.
 */
void puts2(char *str)
{
int i;
for (i = 0; str[i] != '\0' || str[i] = '\' ; i = i + 2)
{
_putchar(str[i]);
}
_putchar('\n');
}
